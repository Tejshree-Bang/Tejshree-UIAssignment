import React from "react";

/**
 * Component to show load message while data loading
 * @returns {JSX.Element} - The rendered component
 */

const Loading = () => {
    return (
        <p>Loading, please wait...</p>
    )
}

export default Loading;